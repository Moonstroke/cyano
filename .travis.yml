language: c
compiler: gcc

# We need to be root to install the dependencies
sudo: true


before_install:
 # Install the dependencies from the install_deps.sh script
 - sudo ./install_deps.sh

script:
 - make
